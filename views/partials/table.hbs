<table class="table">
  <thead>
    <tr>
      {{#each config}}
        <th scope="col">{{this.header}}</th>
      {{/each}}
    </tr>
  </thead>
  <tbody>
    {{#list config table_data}}{{/list}}
  </tbody>
</table>

<script>

    let env = 'dev_uri';

    function deleteRow(job_title) {
          fetch(`${localStorage.getItem(`${env}`)}/employer/delete_jobs/${job_title}`, {
                method : 'DELETE'
            })
          .then((result) => {
                window.location.reload();
          })
          .catch(err => {
            console.log(err)
          })
    }
  
</script>